pipeline {
  agent any

  stages {
    stage ('Build') {
      step {
        sh 'echo "building..."'
      }
    }

    stage ('Test') {
      step {
        sh 'echo "testing..."'
      }
    }

    stage ('Deploy') {
      step {
        sh 'echo "deploying..."'
      }
    }
  }
}
